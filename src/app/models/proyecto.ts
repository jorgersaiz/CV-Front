export class Proyecto {

    titulo: string
    descripcion: string
    github: string
    uid: string

    constructor(titulo: string, descripcion: string, github: string, uid: string){
        this.titulo = titulo
        this.descripcion = descripcion
        this.github = github
        this.uid = uid
    }
}

